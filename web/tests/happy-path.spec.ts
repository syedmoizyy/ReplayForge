import{test,expect}from'@playwright/test';test('inspects a trace, runs a scenario, and opens its report',async({page})=>{await page.goto('/');await expect(page.getByRole('heading',{name:'Event traces'})).toBeVisible();await page.getByRole('button',{name:/Reservation payout/}).click();await expect(page.getByRole('heading',{name:'Replay detail'})).toBeVisible();await page.getByRole('button',{name:/Open divergence report/}).click();await expect(page.getByRole('heading',{name:'Divergence report'})).toBeVisible();await expect(page.getByText('First divergence at event 3')).toBeVisible()});
